# gimme-shelter
Proud React playground application (in black and yellow). Gives a shelter for the most famous detective.
## Build and launch
Install packages: `npm install`.

Run the app: `npm run app`.

Open [localhost:8080](http://localhost:8080) in your browser. 😘
